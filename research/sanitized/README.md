# Sanitized research artifacts

Only synthetic or fully sanitized, non-authenticated examples may be stored here. Executable parser fixtures currently live under `src/fixtures/peopleSoft/` so they are packaged consistently for tests.
